# Dark theme for the dashboard. Streamlit reads this when run from this directory.
[theme]
base = "dark"
primaryColor = "#d9534f"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#1b1f27"
textColor = "#fafafa"
font = "sans serif"

[server]
headless = true
runOnSave = true
