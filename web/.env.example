# Copy to web/.env. The token must match API_TOKEN in the harness .env so the
# frontend can authenticate to the local FastAPI server.
VITE_API_TOKEN=dev-token-change-me

# Optional: point at a non-proxied API origin for a deployed build.
# VITE_API_URL=http://localhost:8000
